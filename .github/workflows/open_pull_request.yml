name: Open Pull Request

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "Automated upstream PR creation is disabled for this fork."
